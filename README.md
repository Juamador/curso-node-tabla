# notas
Este es mi primer programa del curso de note y en un repositorio

```
options:
      --help          Show help                                        [boolean]
      --version       Show version number                              [boolean]
  -b, --base          The table that you want to calculate   [number] [required]
  -l, --listar        Optional parameter to create the file containing the table
                      result                                    [default: false]
      --top, --limit  The limit of the table              [number] [default: 12]
```